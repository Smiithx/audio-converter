# Build tools (required to compile Whisper)
setuptools>=78.1.1
wheel>=0.45.1

# PyTorch stable index with CUDA 11.8 wheels
--extra-index-url https://download.pytorch.org/whl/cu118

# PyTorch, torchvision and torchaudio (stable builds, CUDA 11.8)
torch==2.7.0+cu118
torchvision==0.22.0+cu118
torchaudio==2.7.0+cu118

# Audio and processing dependencies
#numpy>=1.26.0,<2.0.0
numpy>=1.26.0
sounddevice~=0.5.2
scipy~=1.15.3

# Whisper model (latest stable release from PyPI)
openai-whisper>=20250625

# Google Generative AI SDK for video analysis
google-genai~=1.73.1

# Environment variable management
python-dotenv~=1.1.0

# Testing tools
pytest~=8.3.5
